<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Outer ring -->
    <item>
        <shape android:shape="oval">
            <size android:width="176dp" android:height="176dp" />
            <solid android:color="@android:color/transparent" />
            <stroke android:width="3dp" android:color="@color/instagram_orange" />
        </shape>
    </item>

    <!-- Inner ring (slightly inset) -->
    <item android:left="3dp" android:top="3dp" android:right="3dp" android:bottom="3dp">
        <shape android:shape="oval">
            <size android:width="164dp" android:height="164dp" />
            <solid android:color="@android:color/transparent" />
            <stroke android:width="1dp" android:color="@color/instagram_orange_light" />
        </shape>
    </item>

</layer-list>
